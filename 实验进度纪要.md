# ConceptSkillCDM 模块与消融进展总结

> 截止：2025-12-08 本轮对话

---

## 1. 模型模块现状概览

当前（这一轮对话结束时），模型涉及或曾经涉及的关键模块：

1. **Soft Prototype（软原型模块）**  
   - 作用：为学生或试题学习一组原型向量，用于聚类式表示 / 诊断。  
   - 状态：**保留**，是当前模型的核心模块之一。  
   - 消融情况：  
     - 通过 `ablate_soft_prototype=True/False` 做过多轮消融；  
     - 移除后在 ASSIST_09、Junyi 上 AUC 有明显下降，证明其必要性。

2. **Skill Encoder（技能编码器）**  
   - 作用：显式利用 Q 矩阵 / skill id，将试题映射到概念空间，建模学生-技能关系。  
   - 状态：**保留**，对 ASSIST_09 尤其关键。  
   - 消融情况：
     - ASSIST_09 上单独去掉 Skill Encoder，AUC 会崩到 0.66 左右；  
     - Junyi 上影响相对较小，但仍有可见性能损失；  
     - 多次消融表明：Skill Encoder 与 Soft Prototype 有耦合，不能任意拆一个。

3. **G-PDS 个性化关系图（个性化图模块）**  
   - 作用（设计初衷）：为每个学生构建个性化试题关系图，强调个体化行为模式（Personalized Graph）。  
   - 状态：当前代码中仍有模块实现与 flag（`use_personal_graph` 等），但：  
     - 之前的最佳结果 **并没有真正启用 G-PDS（use_personal_graph=0）**；  
     - 最新一轮排列组合消融显示：`g=1`（打开个性化图）时，AUC 反而变差。  
   - 计划：**本轮任务是物理删除整个 G-PDS 模块**（不是仅 ablation）。

4. **Exercise Graph（习题图 / 题目图）**  
   - 作用：基于题目间关系构建图（共现、相似度等），用 GNN 聚合特征。  
   - 早期结论：
     - 消融结果：去掉习题图在 ASSIST_09 和 Junyi 上几乎无损，甚至略有提升；  
     - 推断：图构建或 GNN 设计引入了噪声 / 过平滑。  
   - 状态：**已物理删除** —— 源码中不再有 exercise_graph 相关结构和分支，只有基本嵌入。

5. **Student Factor（学生因子 / 低秩学生-概念因子）**  
   - 作用：用额外低秩因子表示学生 × 概念的个体差异。  
   - 早期消融：
     - ASSIST_09：`ablation_no_stufactor` 的 AUC 达到 0.7715，是当时最优；  
     - 表明这个模块在稀疏数据上可能偏向过拟合。  
   - 状态：**已物理删除** —— 不再有 student_factor_rank / use_student_factor 等字段。

6. **Concept Vector / Concept Matrix 相关路径**  
   - 包括：concept_vector、concept_matrix 等早期尝试。  
   - 状态：**已物理删除**，仅保留 Q 矩阵本身参与 skill encoder 与统计分析。

---

## 2. 已完成的主要尝试时间线

### 2.1 初始消融（只关单一模块）

- 目标：先粗看哪些模块对性能有明显贡献，哪些接近无效或有负面影响。
- 操作：
  - 对 Soft Prototype / Skill Encoder / Exercise Graph 分别做单模块 ablation：
    - `ablation_full`：全部开启；
    - `ablation_no_proto`：关 soft prototype；
    - `ablation_no_skill`：关 skill encoder；
    - `ablation_no_exgraph`：关习题图；
    - `ablation_minimal`：基本模块全部关闭，只保留简单结构；
    - `ablation_no_stufactor`：关闭 student factor。
- 代表结果：
  - **ASSIST_09**
    - `ablation_no_skill`: AUC ≈ 0.665 → Skill Encoder 不可缺。
    - `ablation_no_proto`: AUC ≈ 0.719 → Soft Prototype 也非常关键。
    - `ablation_no_exgraph`: AUC ≈ 0.770，比 full 略高 → 习题图价值存疑。
    - `ablation_no_stufactor`: AUC ≈ 0.7715 → 去掉 student factor 反而略好。
  - **Junyi**
    - `ablation_full`: AUC ≈ 0.816 → 当时整体最优。
    - 去掉 skill / proto 都会有明显降幅，但程度略小于 ASSIST_09。

> 阶段结论：  
> - 必保留：Soft Prototype + Skill Encoder；  
> - 明显负收益：Exercise Graph、Student Factor（至少在当前实现与数据上）；  
> - G-PDS 当时没有真正打开过（use_personal_graph=0）。

---

### 2.2 物理删除无效模块（第一轮大手术）

- 目标：把已经判定“长期负收益”或“项目复杂度过高且收益不明”的模块，从代码层面物理删除，简化结构。
- 删除对象：
  1. **Exercise Graph（习题图）**
  2. **Student Factor（学生因子）**
  3. **Concept Vector / Concept Matrix 相关尝试路径**
- 具体动作（由 Codex 辅助生成修改，再手动检查）：
  - `src/model.py`：
    - 删除习题图相关 nn.Module 子结构与 forward 分支；
    - 删除 student factor 相关参数与低秩因子计算；
    - 把 forward 统一简化为：只依赖学生 id、试题 id（+ Q 矩阵等基本信息）。
  - `src/dataset.py`：
    - `__getitem__` 简化为 `(student_id, exercise_id, label)` 三元组，不再返回概念向量。
  - `src/trainer.py`：
    - 删掉所有 concept_vector / exercise_graph / student_factor 相关的 loss 组合与日志输出；
    - 训练/验证/测试解包统一基于三元组 batch。
  - `src/config.py` / `main.py` / 各种 `run_*.py`：
    - 移除 `use_exercise_graph`、`ablate_exercise_graph`、`student_factor_rank`、`use_student_factor` 等参数；
    - 更新 BEST_CFG、命令行解析和日志打印。
  - `results/*.csv` 写入逻辑：
    - 新写入的结果中，不再包含上述字段的列名。

- 自检：
  - 全局 `grep` 确认上述关键字只剩历史 git 日志中有；
  - 跑 `python main.py --dataset_name assist_09/junyi --epochs 1`，保证训练 & 推理流程完整可跑。

> 阶段结论：  
> 代码成功瘦身，结构更干净，可继续在此基础上做新的消融。

---

### 2.3 针对 Soft Prototype + Skill Encoder 的系统消融（无 G-PDS）

- 编写了一个消融脚本 `run_ablation_full.py`（版本 1）：
  - 使用当前 **BEST_CFG** 作为基线：
    - ASSIST_09：batch_size=128, lr=3e-4, dropout=0.2, lambda_sparse=0.05 等；
    - Junyi：batch_size=1024, lr=1e-3, dropout=0.1, lambda_sparse=0.1 等；
  - 在此基础上对两个开关做组合：
    - `full`：两个都开；
    - `no_proto`：只关 soft prototype；
    - `no_skill`：只关 skill encoder；
    - `minimal`：两者都关。
  - G-PDS 在这轮统一设为 `use_personal_graph=False`，即**整体关掉**，但仍然在代码结构中。

- 目的：
  - 在**不引入 G-PDS** 的情况下，确认 Soft Prototype 和 Skill Encoder 的作用是否稳定；
  - 为下一步“是否物理删除 G-PDS”提供对照。

> 阶段结论：  
> Soft Prototype + Skill Encoder 的重要性再次得到验证，minimal 版本性能明显更差。

---

### 2.4 三模块全排列消融（Soft Proto / Skill Encoder / G-PDS）

- 新一轮目标：考虑 G-PDS 也作为一个模块，做三元布尔组合：
  - p ∈ {0, 1} → soft prototype
  - s ∈ {0, 1} → skill encoder
  - g ∈ {0, 1} → G-PDS 个性化图
- 设计了 8 个 model_variant，比如：
  - `abl_p1s1g0`：proto=1, skill=1, G-PDS=0
  - `abl_p1s1g1`：proto=1, skill=1, G-PDS=1
  - `abl_p0s1g0`：proto=0, skill=1, G-PDS=0
  - ……

- 实验现象（汇总）：
  - **ASSIST_09**
    - 最好结果出现在 `p=1, s=1, g=0`（即 G-PDS 关闭）：
      - `abl_p1s1g0`: AUC ≈ 0.7688
    - 打开 G-PDS (`g=1`) 的组合（比如 `abl_p1s1g1`）AUC 明显偏低（≈0.718）。
  - **Junyi**
    - 最稳的是 `g=0` 的组合（`abl_p0s1g0`, `abl_p1s1g0` 等），AUC ≈ 0.813–0.814；
    - `g=1` 的组合（`abl_p1s1g1` 等）AUC 明显低一档（≈0.78 左右）。

- 与旧结果对比：
  - 旧代码上最好的 AUC（带已删模块但通过 flag 关闭）：
    - ASSIST_09：0.7715；
    - Junyi：0.8160。
  - 新精简结构（删除习题图 & 学生因子）+ 三模块消融中最好的一档：
    - ASSIST_09 ≈ 0.7688；
    - Junyi ≈ 0.8143。
  - 差距在 0.002–0.003 量级，基本可归入：
    - 结构差异 + 随机种子 + 训练浮动（不是“明显退化”）。

> 阶段结论：  
> - 打开 G-PDS 几乎在所有组合下都 **负收益**；  
> - 在 Soft Prototype + Skill Encoder 保留的前提下，G-PDS 没有表现出“相辅相成”，反而像噪声源。  
> - 因此可以接受“物理删除 G-PDS 模块”的决策。

---

## 3. 当前决策与下一步计划（本轮对话的结论）

1. **已经物理删除的模块**
   - Exercise Graph（习题图）
   - Student Factor（学生因子）
   - concept_vector / concept_matrix 相关路径

2. **已经确认要保留的模块**
   - Soft Prototype（软原型）
   - Skill Encoder（技能编码器）

3. **当前正在执行的任务**
   - 通过专门的 Codex 提示词，**物理删除 G-PDS 个性化关系图模块**：
     - 删除 `use_personal_graph`、`lambda_sparse_personal` 等参数；
     - 删除 G-PDS 子模块实现及其 forward 分支；
     - 删除 G-PDS 相关 loss、日志与结果字段；
     - 保证 main.py + config + trainer + model 在新结构下仍可 1 epoch 跑通 ASSIST_09 和 Junyi。

4. **现阶段目标**
   - 先把结构瘦身到：**「Soft Prototype + Skill Encoder + 基础嵌入 + 全局稀疏/原型正则」** 这一条主干；
   - 在这个 **干净、没有多余分支** 的架构上，再去做：
     - 更系统的超参调整；
     - 更有条理的后续消融 / 对比实验。

> 论文的事情先不管，当前主要任务是：  
> **把不靠谱的模块彻底清理掉，确保后续所有实验都跑在一个简洁、稳定、可控的主干模型上。**
