# 2026-05-18 Rollback Record: recover stable 09 mechanism baseline

## Current head before rollback

- Local branch: `master`
- Current HEAD before rollback: `5d2f88634a89ebb98ec3998dbeed253cf546be5d`
- Relevant recent commits after the stable point included CRG/LCRF, route-map, reliable-evidence probes, canonical reachability dataset registration, and full-run diagnostic toggles.

## Why rollback

The current code path did not recover the previous ASSIST09 performance even when using the original benchmark split and the `best_configs.py` profile.

Failed/current run:

- Server: `10.154.22.11`
- Run id: `orig_split_full_best_gpu2_20260518_123815`
- Dataset: `assist_09`
- Variant/profile: `full`, `best`
- GPU: `2`
- Test AUC: `0.7704783186726352`
- Test ACC: `0.7348991974946173`
- Test RMSE: `0.4209220516915773`
- Best val AUC: `0.769997331162676`
- Best epoch/model epoch: `28`
- Save dir: `checkpoints/abce_diag/orig_split_full_best_gpu2_20260518_123815/assist_09/seed42/best_full`
- Log dir: `logs/abce_diag/orig_split_full_best_gpu2_20260518_123815/assist_09/seed42/best_full`

This is below the previous stable ASSIST09 mechanism result, where the same story artifacts showed:

- `full`: AUC `0.7783116166343362`
- `no_A`: AUC `0.767132729913772`
- `no_E`: AUC `0.7633669337459796`
- `E_shuffle`: AUC `0.5750942006494523`
- `E_mean`: AUC `0.6064734561768834`

## Rollback target

Use `ed553d3` as the recovery target, not `f71b90a`.

Reason:

- `ed553d3` preserves the 09 mechanism artifacts under:
  - `results/ae_case09_a95c3c4_standard/`
  - `results/suffnec09_ba2d5c8/`
- `f71b90a` is the child commit titled `Refactor interpretable A/E boundaries`, but it deletes those completed 09 result and figure artifacts.

## Working story to preserve

The target story remains:

- Main problem: Concept Reachability under Sparse Response Evidence.
- A module: Concept Reachability Graph (CRG), a train-only global map that connects historical concepts to currently queried concepts.
- E module: Learner-Conditioned Reachability Filter (LCRF), a personalized local filter that decides which reachable paths are credible for the current learner.

The rollback is intended to recover the strongest working code/results first, then adapt only the naming, diagnostics, and paper-facing framing toward CRG/LCRF without destabilizing the model.

## Next verification after rollback

1. Restore tracked code/results to `ed553d3` in a new commit on `master`.
2. Push and pull on the server.
3. Re-run ASSIST09 `full/no_A/no_E` first.
4. Proceed to `junyi`, `assist_17`, and `nips34` only after ASSIST09 recovers near the previous `0.778` AUC.
5. Keep future CRG/LCRF edits small and data-driven: first profile dataset/reachability, then change code only if the profile explains why the module is weak.
