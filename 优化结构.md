直接把“能落地的优化动作”给你拎出来，按 **Assist09 / Junyi / 通用** 来拆。你可以当成一个 TODO 清单。

---

## 一、总体决策逻辑（你要怎么选）

* Assist09：**优先 = 修改核心结构，其次才是删冗余，不建议随便加复杂模块**

  * 关键词：图结构（GNN）、单调性交互、Soft Q、瘦身网络

* Junyi：**优先 = 增加时序 & 文本模块，同时顺手删无效结构**

  * 关键词：时序动态、遗忘曲线、文本嵌入、抗噪建模（guess/slip）

---

## 二、针对 Assist09（目标 AUC ≥ 0.800）——优先做的事

### 1. **修改模型结构：引入图结构 / 图嵌入**

1）从独立 embedding → 图嵌入（GNN Embedding）

* 构建 **学生-习题二部图**：节点=学生/习题，边=交互（含答题结果）。
* 用 GCN / GAT / LightGCN 等做消息传递：

  * 让“只做了少量题的学生”共享相似学生的信息（协同过滤效应）。
* 预期收益：**+0.015–0.025 AUC** 级别，是冲击 0.800 的主力手段。

> 实质行动：
>
> * 在现有模型里，把 `stu_emb`, `item_emb` 从“纯 lookup”改成“先 lookup，再经 1–2 层 GNN 更新后再用”。

2）改交互函数：从黑箱 MLP → “能力 - 难度”结构化函数

* 把预测从 `sigmoid(MLP([h_u, h_e]))` 改成类似 IRT/NeuralCD 的形式：
  [
  P(y=1) = \sigma(a_e \cdot (\theta_u - \beta_e))
  ]

  * `θ_u`：学生能力向量
  * `β_e`：题目难度
  * `a_e`：题目区分度
* 在代码层面：

  * 用 **线性层 + 约束权重为非负**（如 `weight = softplus(raw_weight)`），保持单调性。

> 目的：**强化“能力越高，概率不跌”的归纳偏置**，提升泛化。

3）加入/强化 **单调性约束**

* 在交互层中对权重做非负约束：

  * 如：`W_pos = F.softplus(W_raw)` 或 `W_pos = torch.abs(W_raw)`
* 避免模型学到“能力高反而预测低”的反直觉模式，尤其对 Assist09 这种稀疏数据重要。

---

### 2. **增加模块（针对 Assist09 但优先级次于结构）**

1）Soft Q / Q 矩阵修正模块

* 在专家 Q 矩阵基础上，学习一个小的残差：

  * [
    Q_{\text{final}} = \text{Clip}(Q_{\text{expert}} + \Delta Q, 0, 1)
    ]
* 让模型自动微调题-知识点关联，修正人工标注误差。

> 预期收益：**+0.005–0.010 AUC**，属于性价比不错的“微调上限”手段。

---

### 3. **删除无效结构 / 瘦身（Assist09 必做的减法）**

1）剪掉几乎没贡献的复杂模块

* 典型情况（从消融看）：

  * 去掉某 Attention / 某深层 MLP → AUC 变化 < 0.001，甚至略涨
* 操作：

  * 保留 1–2 层必要的交互层；
  * 对冗余的多头注意力、多层 MLP、复杂辅助分支直接删除。

2）控制 GNN / MLP 深度

* GCN / GAT：**限制在 1–2 层**，避免 over-smoothing。
* MLP：

  * 减少隐藏层数和维度，不要为了“看起来深”硬堆深度。

3）删除效果极弱的辅助任务 / 辅助损失

* 如果某些 auxiliary loss（重构、对比学习等）的 λ 调来调去，对 AUC 几乎没影响：

  * 建议直接移除该辅助头和对应 loss 项。
* 目的：**减轻梯度冲突，让主任务（答题预测）更干净**。

---

## 三、针对 Junyi（目标 AUC ≥ 0.823）——要“加料”的地方

### 1. **增加时序动态模块（高优先级）**

1）显式建模学习与遗忘（Temporal Dynamics）

* 在学生状态中引入时间：

  * 把每次交互的时间戳 ( t ) 和间隔 ( \Delta t ) 作为输入。
* 可以选的形式：

  * 轻量 LSTM / GRU / Transformer encoder
  * 或者显式“遗忘函数”：
    [
    \theta_{u,t} = \theta_{u,t-1} + \Delta \text{Learning} - \Delta \text{Forgetting}(\Delta t)
    ]
* 实现层面：

  * 给每条交互额外特征：`delta_t`，过一层 `f(delta_t)`（如 `exp(-λ * delta_t)`）后，用作对历史记忆的衰减因子。

> 预期收益：对 Junyi **+0.015–0.02 AUC** 量级，是冲击 0.823 的核心模块之一。

---

### 2. **增加内容感知 / 文本模块（中高优先级）**

1）题目文本嵌入（Text Embedding）

* 用预训练语言模型（如 BERT/DistilBERT 或轻量中文模型）编码题干、选项等，得到 `v_text`。
* 与题目 ID embedding `v_id` 融合：

  * 拼接后过线性层，或
  * 加权和：`v_final = LayerNorm(W1 * v_id + W2 * v_text)`

> 作用：
>
> * 解决“长尾题目”数据少的问题，用语义相似性补信息。

2）难度/特征辅助任务（可选）

* 以题目平均正确率等指标，添加一个**难度预测辅助头**：

  * 逼迫题目向量学到更稳定的“难度 / 区分度”。

---

### 3. **增加抗噪与 Guess/Slip 建模**

1）显式建模猜测率 g_e 与失误率 s_e

* 输出层形式：
  [
  P_{\text{final}} = (1 - s_e) P_{\text{model}} + g_e (1 - P_{\text{model}})
  ]
* 约束 g_e, s_e 在合理范围（如 [0, 0.3]），防止模型用它们“糊弄”误差。

> 用意：让主参数 (\theta) 不被噪声行为（瞎蒙 / 手滑）严重污染。

---

### 4. **对 Junyi 的删减与整理**

* 在大数据集上，不必追求超深结构：

  * 更推荐 **“更宽但不更深”** 的结构。
* 结合消融：

  * 删除对 AUC 贡献很小的深层 MLP、复杂 self-attention 堆叠；
  * 保留关键模块：时序 + 文本 + 核心交互层。

---

## 四、通用优化：损失函数 & 训练策略

### 1. 损失函数

* 主体：仍然是 **BCE**。
* 增强：加入 **pairwise ranking loss** 以直接优化 AUC：
  [
  L = L_{\text{BCE}} + \lambda \sum_{i,j} \max(0, P(u,i) - P(u,j) - \epsilon)
  ]

  * i 为应当更易做对的题，j 为更难题。

> 效果：让模型不仅“预测对”，还要“顺序排对”，直接对 AUC 有帮助。

### 2. 优化器与 lr 策略

* 优化器：**AdamW**（适合 GNN + 文本 +多模块）。
* 学习率：

  * 建议 **Cosine Annealing with Warm Restarts**，防止卡在坏的局部极小值。

### 3. 对抗训练（可选加分项）

* 在 embedding 层引入小扰动（FGM/PGD 类方法），提升鲁棒性。
* 对干净数据 + 微扰数据共同训练。

---

## 五、总结成一句话的执行顺序

* **Assist09：先改成 GNN + 单调性交互 → 加 Soft Q → 删冗余深层 / 辅助损失。**
* **Junyi：在现有架构上先上时序动态 → 再上文本嵌入 → 同时显式建模 guess/slip → 顺手瘦掉没用的复杂结构。**

如果你愿意，下一步我可以结合你现有的 `model.py` / 训练脚本，把这些优化点翻译成**具体改哪些 class / 增加哪些参数 / 怎么做消融验证**的更细 TODO。
